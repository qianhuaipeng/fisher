spring:
  application:
    name: fisher-gateway-service
  cloud:
    nacos:
      config:
        server-addr: 172.16.1.212:8848
        file-extension: yml
        group: GATE_GROUP
  profiles:
    active: dev